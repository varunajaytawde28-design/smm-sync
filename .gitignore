# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
env/

# SMM-Sync runtime state
.smm/events.jsonl
.smm/state.json
.smm/parsed_context.json
.smm/locks/
.smm/history/

# Context graph database — never commit, run `smm seed-graph` to populate
.smm/graph/

# Capture state — tracks last-seen IDs, not human config, do not commit
.smm/capture_state.json

# Compliance audit log — append-only, never commit (contains agent session data)
.smm/compliance_lineage.jsonl

# Dashboard runtime state — never commit
.smm/killed_sessions.json
.smm/contradictions.jsonl

# pytest
.pytest_cache/
.coverage
htmlcov/

# macOS
.DS_Store

# IDE
.idea/
.vscode/
